{
  "name": "Decidim Monitor",
  "description": "A tool to monitor decidim instances.",
  "repository": "https://github.com/codegram/decidim-monitor",
  "formation": {
    "web": {
      "quantity": 1,
      "size": "hobby"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "docker login --username=_ --password=$HEROKU_AUTH_TOKEN registry.heroku.com",
        "test": "docker build -t registry.heroku.com/decidim-monitor/web . && docker push registry.heroku.com/decidim-monitor/web"
      }
    }
  }
}
